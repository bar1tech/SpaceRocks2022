/// @description

// change the button position slightly so that it appears to move when clicked.
// ystart stores the initial y position of the instance when it is first created in the room.
y = ystart + 4;

// add sound effect for button click here

// the alarm event runs after 10 frames and resets the position of the button
alarm[0] = 10;
